<h1><%= @category.name %></h1>
<%= link_to "Edit", edit_category_path(@category) %>

<%= render @products %>

<%= link_to 'Сортировать', sorting_categories_path(category_id: @category.id) %>
<%= link_to 'Очистить сортировку', clear_sorting_categories_path(category_id: @category.id) %>

